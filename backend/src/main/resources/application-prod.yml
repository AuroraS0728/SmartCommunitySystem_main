server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://mysql:3306/smart_community?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: smart_user
    password: smart_password
  data:
    redis:
      host: redis
      port: 6379
      database: 0
      timeout: 3s
